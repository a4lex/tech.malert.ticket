import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const SignupScreen = () => {
  return (
    <View>
      <Text style={{fontSize: 48, alignSelf: 'center', paddingTop: 60}}>
        SignupScreen
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SignupScreen;
